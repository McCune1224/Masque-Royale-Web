// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.26.0
// source: role_passives_join.sql

package models

import (
	"context"
)

const getRolePassiveDetails = `-- name: GetRolePassiveDetails :many
SELECT pd.id, pd.name, pd.description
FROM role_passives_join rpj
JOIN passive_details pd ON rpj.passive_id = pd.id
WHERE rpj.role_id = $1
`

func (q *Queries) GetRolePassiveDetails(ctx context.Context, roleID int32) ([]PassiveDetail, error) {
	rows, err := q.db.Query(ctx, getRolePassiveDetails, roleID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PassiveDetail
	for rows.Next() {
		var i PassiveDetail
		if err := rows.Scan(&i.ID, &i.Name, &i.Description); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
